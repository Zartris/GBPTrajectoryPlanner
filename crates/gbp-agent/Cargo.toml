[package]
name    = "gbp-agent"
version = "0.1.0"
edition = "2021"

[dependencies]
heapless   = { workspace = true }
libm       = { workspace = true }
gbp-core   = { path = "../gbp-core" }
gbp-comms  = { path = "../gbp-comms" }
gbp-map    = { path = "../gbp-map" }
