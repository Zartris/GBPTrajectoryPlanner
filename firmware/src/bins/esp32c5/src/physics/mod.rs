// Physics task — arc-length integration (s += v·dt).
// Reads velocity from the GBP agent and updates the robot's position
// along the current edge, advancing to the next edge when the end is reached.
